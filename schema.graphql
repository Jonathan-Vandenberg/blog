type Query {
  userMessages: [UserMessage]
}

type Mutation {
  addUserMessage(input: UserMessageInput!): UserMessage
}

type UserMessage {
  id: ID!
  email: String!
  name: String!
  message: String!
}

input UserMessageInput {
  id: ID!
  email: String!
  name: String!
  message: String!
}
