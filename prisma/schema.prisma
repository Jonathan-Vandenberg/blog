generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGODB_URI")
}

model UserMessage {
  id      String @id @default(auto()) @map("_id") @db.ObjectId
  email   String
  name    String
  message String
}
